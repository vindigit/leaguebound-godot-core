# Roster pairing construction evidence

The original population-construction source is frozen commit `8b8fd437ca700beef14886b61f95f7ce9eb77c93`.
Godot is `4.7.1.stable.official.a13da4feb`. This evidence concerns fixture
construction only; it does not certify game outcomes or §27.1.

`construction_audit.json` was generated by
`Godot --headless --path . -s tools/audit_population_pairing.gd`.
`construction_audit_metadata.json` records source hashes and command provenance.
The archive contains all 117 existing rounded 10-player × 20-rating roster
vectors, with actual offensive/defensive capability indices, per competition.
"Legacy" means the old adjacent pairing applied to these unchanged rosters,
not a run of an old engine.

The old raw ladder difference has two values: +2 steps in 11/13 cases and −11
steps in 2/13, or +0.70 and −3.85 rating-level points. This describes the raw
team tilt, not each rounded player rating or complete capability gap. Across
234 old fixtures (the full roster period crossed with opening parity), actual
capability gaps have 198 positive and 36 negative values, zero ties, and mean
zero in every competition. Thus equal average strength alone did not prove
venue exchangeability.

The corrected full 54,756-fixture joint cycle has 27,144 positive and 27,144
negative gaps, 468 ties, and mean zero in every competition. Root-mean-square
capability gaps change as follows; no target or tolerance was used to choose
these values.

| Competition | Legacy RMS | Corrected RMS |
| --- | ---: | ---: |
| High school | 4.410762 | 4.972918 |
| College | 4.427761 | 5.052585 |
| Development | 4.405831 | 4.968797 |
| Overseas | 4.405831 | 4.968797 |
| Top domestic | 4.388656 | 4.962553 |

Every aligned quartet crosses its actual two rosters over both venue and
opening status. An aligned 468-fixture block has uniform full-roster marginals
but only 231 ordered roster cells and 25 of 169 ladder-pair cells after both orientations. The 117 base orientations alone cover 117 roster cells and 13 ladder cells. It is not
an independent local joint population. Partial quartets need not balance;
explicit venue offsets and fixed-opener diagnostic arms intentionally override
these symmetries. The construction proof makes no home-win, overtime, scoring,
or pace claim.

`tests/calibration/test_population_pairing.gd` tests all 13,689 ordered pairs,
all aligned 117-pair marginals, exact quartet orientation, actual rounded
roster/capability exchangeability in all five competitions, the existing
117-state roster period, eligibility, repeatability, explicit interventions,
direct mirror preservation, and large variation boundaries with input identity
and profile preservation. Custom inline runner consumers have explicitly
structural source-wiring assertions; these are not runtime execution claims
about those runners.

The isolated mutation command is
`pwsh -NoProfile -File tools/run_population_pairing_mutations.ps1 -GodotBin <pinned Godot executable>`.
The final invocation adds `-OutputDirectory analysis/roster_pairing_followup/verified_mutations`. Its `verified_mutations/results.json` records the baseline, actual-source mutation
outcomes, restored source hashes, and retained isolated copy. Mutation changes
never touch the shared checkout. Logs with assertion failures are expected
mutation evidence, not green runs.

Two earlier attempts remain explicitly superseded: the first lacked actual reverse-orientation union assertions; the second completed all assertion kills but failed exact restoration because the harness removed a UTF-8 BOM. The corrected runner restores original bytes. That historical eight-case run was accepted at its recorded source state; the final nine-case source is covered by verified_mutations_v2 below.

The corrected verified_mutations run completed successfully: baseline 8/8, zero errors/failures/skips/flaky/orphans; all eight actual-source mutants exited 100 with assertion failures and zero errors. The runner verified exact restored-byte SHA256 equality for the catalog, all three custom consumers, and the final test source. Its recorded completion head is bf275ccc89e16fe75830fd34a37529faf8df610a; immutable behavior and tested-source hashes are recorded separately in the evidence.

## Final consumer-boundary coverage

Subsequent review found that the migrated home-court consumer narrowed a mirror
index to 32 bits. The final consumer preserves the original int64 scalar index;
the population mapping is unchanged. The ninth test executes 280 actual consumer
inputs across all five competitions, both modes, orientations and environment
arms, with seven boundary/large-index witnesses. The preserved faulty consumer
fails precisely at variation 1,073,741,824.

The final mutation invocation uses
`-OutputDirectory analysis/roster_pairing_followup/verified_mutations_v2`.
At source head `7e4a73128f7c64b786679d40dee064cb2b40994e`, its baseline passes
9/9 with zero errors, failures, skips, flaky cases or orphans. All nine actual
source mutants are assertion-killed with exit 100 and zero errors. Earlier
mutants can stop before all nine cases execute; the new narrowing mutant runs
all nine and fails only the boundary case. All five source hashes match both
the shared checkout and the restored isolated copy byte-for-byte.

Independent final-source acceptance and its exact inventory are archived in
`mirror_boundary/INDEPENDENT_V2_REVIEW.md` and
`mirror_boundary/independent_v2_review.json`. The earlier eight-case artifacts
remain historical evidence, not the final-source test claim. Complete-gate,
population, replay and publication acceptance are separate.
